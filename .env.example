# Copier vers .env : `cp .env.example .env` (optionnel, le compose a des valeurs par défaut)
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432
KAFKA_HOST_PORT=9092
# Clé HMAC (Base64, >= 32 octets décodés) — générer : openssl rand -base64 32
JWT_SECRET=B7lNty52nURS1lCg6KjEvPh6e71c/ndOh1H4mCMRMgo=
