SPRING_PROFILES_ACTIVE=docker
DB_HOST=localhost
DB_PORT=5432
DB_NAME=payment_db
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_LIQUIBASE_USERNAME=postgres
DB_LIQUIBASE_PASSWORD=postgres
KAFKA_HOST=localhost
KAFKA_PORT=9092
JWT_SECRET=B7lNty52nURS1lCg6KjEvPh6e71c/ndOh1H4mCMRMgo=
PAYMENT_SERVER_URL=http://localhost:8090
COMMISSION_RATE=0.1

# Clé API pour /api/v1/wallets et /api/v1/transactions (alignée avec docker-compose racine)
PAYMENT_INTERNAL_API_KEY=dev-payment-internal-key-min-16chars
EXPOSE_OPENAPI=true
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3999
MAX_TRANSACTION_AMOUNT=1000000

# Redis (cache solde, désactivé en local si CACHE_REDIS_ENABLED=false)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# CACHE_REDIS_ENABLED=false
# CACHE_WALLET_BALANCE_TTL=PT30S

# Pool R2DBC (optionnel)
# R2DBC_POOL_MAX_SIZE=5
# R2DBC_POOL_MAX_ACQUIRE_TIME=30s
